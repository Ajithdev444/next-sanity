import page from "./page-schema"
import project from "./project-schema"
import gallery from "./gallery-schema"
import team from "./team-schema"

const schemas = [project, page, gallery,team]

export default schemas